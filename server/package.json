{
  "name": "naipe-azul-api",
  "version": "1.0.0",
  "description": "Backend mínimo do checkout Naipe Azul — cria cobranças Pix/Cartão na Asaas. A chave da API nunca fica no front-end.",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "dev": "node --watch --env-file-if-exists=.env index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "helmet": "^8.3.0",
    "cors": "^2.8.5",
    "pdfkit": "^0.15.1"
  }
}
