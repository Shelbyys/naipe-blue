# Build do site estático (Dockerfile na raiz) — não precisa do
# backend nem do histórico do git.
server
.git
Dockerfile
.dockerignore

# Material interno (pitch de investidor) — fica versionado no git, mas
# não precisa ser servido publicamente no domínio do site.
pitch.html
